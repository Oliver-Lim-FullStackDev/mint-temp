import React from 'react'
import './styles.css'

type NextLayoutProps = import('.next/types/app/(frontend)/layout').LayoutProps

export const metadata = {
  description: 'A blank template using Payload in a Next.js app.',
  title: 'Payload Blank Template',
}

export default async function RootLayout(props: NextLayoutProps) {
  const { children } = props

  return (
    <html lang="en">
      <body>
        <main>{children}</main>
      </body>
    </html>
  )
}
