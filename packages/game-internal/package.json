{
  "name": "@mint/game-internal",
  "version": "0.1.0",
  "description": "Internal Mint game runtime and slot engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "pnpm --filter @mint/gamestudio-api build",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mint/gamestudio-api": "workspace:*"
  }
}